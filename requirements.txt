# Core
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
python-dotenv>=1.0
tqdm>=4.66

# LLM API (GPT-5.4 via OpenAI-compatible endpoint; also works for local vLLM)
openai>=1.30

# RoBERTa SLM baseline (step3) — install the CUDA build of torch that matches
# your 4090 driver, e.g. from https://pytorch.org for cu121.
torch>=2.1
transformers>=4.38

# ARG repo has its own requirements.txt (python==3.10, CUDA 11.3); install it
# separately inside the cloned ARG directory.
